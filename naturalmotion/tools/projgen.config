#
# Project Generator config file that indicates that within this folder and below
# projects should be built to the project generator directives in the single file specified below.
# - as used when batch recursively generating projects.
#

%RS_TOOLSROOT%/etc/projgen/sample.build